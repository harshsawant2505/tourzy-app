import { View, Text, TextInput } from 'react-native'
import { CgProfile } from "react-icons/cg";
import React from 'react'

const Input = ({value, setFunc}:any) => {
  return (
    <View className='w-full bg-white h-2 flex-row'>
       


    </View>
  )
}

export default Input